<?php 

/*  */

include 'explode.php';

function fn_substr($str, $offset, $length=null){
    
    for ($i=0; $i < fn_strlen($tab); $i++) { 
        if ($tab[$i] == fn_strlen($tab)){
            return $tab[$i];
        }
    }
}
$str = "coucouçava";
echo fn_substr($str, -1);